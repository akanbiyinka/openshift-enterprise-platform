version: "3.9"

services:

  openshift-app:

    build:
      context: ./app

    container_name: openshift-app

    ports:
      - "8080:8080"

    restart: unless-stopped

    environment:
      APP_NAME: OpenShift Enterprise Platform
      APP_VERSION: "1.0.0"